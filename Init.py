"""FreeCAD application-side initialization for ASA Chain Drive.

FreeCAD adds the module directory to ``sys.path`` before executing this file.
The file deliberately has no GUI imports so headless FreeCAD remains supported.
"""
